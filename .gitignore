# Secrets — never commit
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
.idea/
.vscode/

# pytest / runtime artifacts
.pytest_cache/
snapshots/

# real-map world (regenerate via scripts/fetch_osm.py — never commit the graph)
data/*.graphml
cache/

DEMO_GUIDE.txt
# Downloaded design bundles (reference only; re-fetch from the design URL)
design_dl/
