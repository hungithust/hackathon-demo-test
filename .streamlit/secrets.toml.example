# Copy this file to `.streamlit/secrets.toml` for LOCAL runs (it is gitignored),
# OR paste the same line into Streamlit Cloud → App → Settings → Secrets.
# Without a key the app still runs using scripted reasoning.

ANTHROPIC_API_KEY = "sk-ant-..."
